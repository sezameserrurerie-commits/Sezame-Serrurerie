const navToggle=document.querySelector(".nav-toggle");const mainNav=document.querySelector(".main-nav");if(navToggle&&mainNav){navToggle.addEventListener("click",()=>{const isOpen=mainNav.classList.toggle("is-open");navToggle.setAttribute("aria-expanded",String(isOpen))});mainNav.addEventListener("click",event=>{if(event.target instanceof HTMLAnchorElement){mainNav.classList.remove("is-open");navToggle.setAttribute("aria-expanded","false")}})}const contactForm=document.querySelector(".contact-form");if(contactForm){contactForm.addEventListener("submit",event=>{event.preventDefault();const status=contactForm.querySelector(".form-status");const fields=Array.from(contactForm.querySelectorAll("input, select, textarea"));const missingField=fields.find(field=>!field.value.trim());const email=contactForm.querySelector('input[type="email"]');const emailLooksValid=email&&/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.value.trim());if(missingField){missingField.focus();status.textContent="Merci de completer tous les champs avant l'envoi.";return}if(!emailLooksValid){email.focus();status.textContent="Merci d'indiquer une adresse email valide.";return}contactForm.reset();status.textContent="Votre demande est prete. Sezame Serrurerie vous recontactera rapidement."})}